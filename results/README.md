# Results

- **Latest runs** – `comparison_*.json`, `oolong_*.json`, `synthetic_*.json`, `robustness_suite_*.json`
- **Archive** – `archive/` holds older runs for reference

Results are gitignored by default. To keep specific runs, move them out of `results/` or adjust `.gitignore`.
